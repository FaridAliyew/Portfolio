services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: farid-portfolio
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
